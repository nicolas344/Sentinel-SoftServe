apiVersion: v1
kind: Service
metadata:
  name: alertmanager-svc
  namespace: sentinel
  labels:
    app: alertmanager
spec:
  selector:
    app: alertmanager
  ports:
    - port: 9093
      targetPort: 9093
  type: ClusterIP
